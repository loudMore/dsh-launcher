# DeepSeek Harness 启动器 · 更新日志

本文件记录 DeepSeek Harness 启动器（Windows 桌面版）各版本的更新内容。版本号遵循语义化版本规范。

---

## v1.1.0（2026-08-17）

本版本对网络层进行整体重构，统一所有下载与更新通道，重点解决部分环境下网络功能不可用的问题。当前为源码版本，正式发布包随后提供。

### 新增功能

- **统一网络层**：新增独立网络模块，程序所有对外请求（插件商城、版本检查、组件下载、自更新）统一经由同一通道发出，行为一致、可诊断。
- **网络诊断模式**：新增 `--net-probe` 启动参数，输出 TLS 版本、代理探测过程、各下载源逐项连通性报告至 `net-probe.txt`，便于排查网络问题。
- **无界面自动化模式**：新增 `--headless <命令文件>` 启动参数，支持以脚本方式执行环境检测、服务启停、更新检查、商城获取等操作，全程无窗口、无弹窗，供自动化测试使用。
- **命令行强制代理**：新增 `--proxy <地址>` 参数，可强制指定代理地址进行诊断对比。

### 优化改进

- **传输安全升级**：显式启用 TLS 1.2/1.3。此前程序默认仅支持 TLS 1.0，导致在要求更高安全标准的网络环境下所有在线功能不可用，这是本次修复的核心问题。
- **下载源策略调整**：全部下载链改为官方源优先（nodejs.org、GitHub、npm 官方源），失败后依次回退 npmmirror、华为云等镜像源，不再默认绕过官方渠道。
- **代理探测无感化**：代理探测结果按短周期缓存并在每次使用前复验，代理断开后自动回落直连；探测结果不再写入进程环境变量，不影响子进程行为。
- **服务停止以进程树为准**：停止/重启服务时优先结束由启动器自身拉起的进程树，用户修改端口后依然正确；进程树结束改为同步等待，消除"服务未能停止"的竞态。
- **安装后即时可用**：一键安装 Node.js / Git 后立即注入当前会话环境，无需重启程序即可继续安装 dsh。
- **日志并发保护**：多线程写日志加锁，消除偶发的日志写入失败；日志读取改为流式读取尾部，大日志文件不再卡顿界面。

### 问题修复

- 修复插件商城在部分环境下始终为空的问题（根因：TLS 版本过低被服务拒绝）。
- 修复启动器自更新下载失败无明确提示的问题。
- 修复全新环境一键安装时 npm 在同一次安装流程内不可用，导致 dsh 安装失败的问题。
- 移除针对特定机器的路径与端口预设，环境探测仅使用跨机器通用位置，提升在不同电脑上的一致性。

### 说明

- 本版本当前仅提供源码，正式发布包（含自动升级）将在验证完成后发布；已安装 v1.0.9 的用户暂不会收到升级提示。


## v1.0.9（2026-08-16）

本版本进行通用化改造与安全性加固，修复数据目录与进程管理相关问题。

### 新增功能

- **用户数据目录统一**：日志、插件、配置等数据统一存放于用户数据目录（`%LocalAppData%\DeepSeekHarness`），不再随程序所在位置变化。无论程序放置于桌面、系统目录或便携设备，均不会污染桌面或产生写入权限问题。
- **桌面快捷方式自动同步**：程序启动时自动将桌面快捷方式指向当前运行的版本，升级后双击快捷方式即为最新版本。

### 优化改进

- **服务进程安全校验**：停止、重启服务前校验端口占用进程是否为 dsh 服务，避免误杀其他程序；端口被非 dsh 程序占用时不再执行强杀，并给出明确提示。
- **插件安装输入校验**：git 仓库地址仅拒绝命令行元字符（允许 URL 编码字符）；npm 包名按规范白名单校验，防止非法输入。
- **浏览器打开前服务探测**：服务未就绪时不再打开空白页面，改为明确提示。

### 问题修复

- 修复程序位于桌面等位置时，日志与插件目录错误生成在程序所在目录的问题。
- 修复升级后桌面快捷方式仍指向旧版本的问题。
- 修复停止/重启服务可能误杀非 dsh 进程的问题。

### 说明

- 已安装旧版本的用户升级后，原有配置与插件目录自动沿用，无需手动迁移。
- 发布文件已通过 SHA256 校验。

---

## v1.0.8（2026-08-16）

本版本围绕新手使用体验与网络环境适应性进行系统性优化，新增软件自更新能力。

### 新增功能

- **软件自更新**：检测到新版本后，可在更新页一键完成下载、校验、替换与自动重启，全程无需手动操作；升级过程不影响 dsh 服务运行。下载结果优先通过 SHA256 校验，无校验值时回退至程序特征校验；支持官方源与多个国内加速镜像源。
- **一键导出诊断包**：设置页可一键打包运行日志、配置信息（代理密码已自动脱敏）与环境检测报告，便于问题反馈与排查。打包过程不依赖外部组件，输出位置支持多级自动降级。

### 优化改进

- **代理检测机制重构**：优先读取系统代理设置（与浏览器同一数据源），仅在必要情况下探测本地端口，启动速度显著提升。代理状态实时生效：启用代理后立即使用，断开后自动回退直连，不再残留失效代理配置。
- **新手引导完善**：环境页引导面板在任一必要组件（Node.js、npm、Git、dsh）缺失时均会展示，并明确列出缺失项；一键安装按钮在环境不完整时始终可见。
- **一键安装流程优化**：Node.js 与 Git 的安装包解压改为内置实现，不再依赖系统 tar 工具，兼容性提升；Git 安装包新增多个国内镜像源。
- **设置页「自动检测回填」优化**：检测后同步回填当前生效的配置值与代理信息。
- **版本信息统一管理**：版本号集中由单一常量维护，消除多处硬编码，便于后续版本维护。

### 问题修复

- 修复启动时因代理端口探测导致的环境检测长时间等待问题。
- 修复诊断包导出失败的问题（原实现依赖 PowerShell 子进程，且受桌面目录权限影响）。
- 修复一键安装 Node.js 时下载地址解析失败的问题。
- 修复代理误判及失效代理残留导致网络请求异常的问题。
- 修复 npm 安装失败时错误信息不明确的问题。
- 修复日志记录缺失问题，启动检测、设置保存、端口变更等关键操作现均写入结构化日志。

### 说明

- 本版本发布文件已通过 SHA256 校验，校验值与版本信息同步发布。
- 环境检测与一键安装支持无代理（直连）与代理环境自动适配。

---

## v1.0.7（2026-08-16）

本版本重点改进一键安装与升级过程中的错误提示与重试策略。

### 优化改进

- **错误信息明确化**：npm 安装失败时直接展示真实错误原因，并完整写入日志文件，便于定位网络、代理或环境问题。
- **安装重试策略**：依次尝试官方源、国内镜像、清除代理后重试，覆盖代理配置残留导致全部源不可用的场景。
- **安装过程优化**：增加安装加速参数与超时限制，避免网络异常时长时间无响应。
- **安装结果核验**：安装完成后校验 dsh 命令是否真实可用，防止误报成功；npm 全局目录实时写入环境变量，安装后无需重启即可使用。

### 问题修复

- 修复 npm 安装失败时统一误报「超时」的问题，现显示真实失败原因。

---

## v1.0.6（2026-08-15）

本版本将插件自检机制由静态预检升级为多级防线。

### 新增功能

- **插件自检三道防线**：
  - 静态依赖预检：启动前扫描插件依赖，缺失时自动补齐，无法修复的插件先行隔离；
  - 日志巡检：服务就绪后扫描运行日志，识别加载期错误并定位问题插件；
  - 前端资源探测：检测插件前端资源加载状态，避免单个插件异常影响整体界面。
- 自检结果统一提示，并附修复建议。

### 问题修复

- 修复前端资源探测误判：无前端界面的服务端插件不再被误隔离。

---

## v1.0.5（2026-08-15）

本版本新增启动过程可视化与智能环境定位。

### 新增功能

- **启动步骤指示**：一键启动时显示检查插件、启动服务、就绪三个步骤的状态与进度。
- **智能环境定位**：检测不到 dsh 时提供引导入口，支持深度扫描、手动选择文件、指定目录自动查找，兼容源码安装方式。

### 问题修复

- 修复窗口最小化及从托盘恢复后部分区域不重绘的问题。

---

## v1.0.4（2026-08-15）

### 问题修复

- 修复插件批量更新时误列出已是最新的插件，现仅处理确有更新的插件。
- 修复本地分支与远程默认分支不一致时的误判，此类场景不再误报连接失败。

---

## v1.0.3（2026-08-15）

### 新增功能

- 环境页新增引导面板，提供智能定位与手动定位 dsh 的多种方式。

### 问题修复

- 修复窗口最小化及恢复后的渲染异常问题。

---

## v1.0.2（2026-08-15）

本版本进行交互一致性与稳定性修复。

### 优化改进

- 托盘菜单主题切换全局生效，切换后立即重建并展示新主题。
- 插件更新判定逻辑统一：插件页与更新页采用一致的更新判定策略，仅在确实存在更新时提示。
- git 操作自动适配代理设置。

### 新增功能

- 设置页新增「关于」对话框，介绍软件定位、核心功能与支持的语言。

### 问题修复

- 修复托盘菜单部分操作异常导致程序退出的问题。
- 修复更新页版本号显示不一致的问题。

---

## v1.0.1（2026-08-15）

本版本引入插件依赖管理系统，解决插件依赖缺失导致的连锁故障。

### 新增功能

- **插件依赖自动管理**：插件安装后自动解析并补齐依赖，缺失依赖的插件自动隔离，保证服务正常启动。
- **依赖状态展示**：插件列表显示依赖完整性状态，并提供一键修复入口。
- **共享依赖目录**：插件间复用公共依赖，减少重复安装。

### 问题修复

- 修复插件依赖缺失导致的服务端插件树崩溃及前端资源加载失败问题。
- 修复全局 npm 目录探测及作用域包依赖解析问题。

---

## v1.0.0（2026-08-15）

正式版首发，基于 WPF 全面重构。

### 界面与交互

- WPF 重写，支持任意 DPI 高清显示与无边框窗口。
- 深色 / 浅色双主题，支持实时切换。
- 现代化托盘菜单、对话框与日志查看界面。

### 主要功能

- 一键安装：自动下载并安装 Node.js，写入环境变量。
- 服务管理：启动、停止、重启及状态实时监控。
- 插件管理：安装、更新、卸载、依赖修复与一键维护。
- 插件商城：聚合多个插件源，支持搜索、排序与一键安装。
- 多语言支持：中文、英文、日文等。

### 说明

- 旧版 WinForms 试验版本归档，不再维护。
