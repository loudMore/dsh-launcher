# 更新日志 / Changelog

## v1.0.8 (2026-08-16)

**新手引导醒目化 + 全环境一键补齐 + 公共镜像策略 + 软件自更新**

### 🚀 软件自更新（不再把用户推去 GitHub）

- 更新页「启动器」卡片：发现新版本后按钮变为「🚀 立即升级 vX.X.X」，点一下即自动【下载（带进度条）→ 校验 → 替换 → 自动重启升级】，全程无需用户手动操作
- 校验双保险：优先 SHA256（version.txt 附带 sha256=），无哈希时退回 PE 校验（MZ 头 + UTF-16 版本字符串 + 程序特征名）
- 下载多镜像链：GitHub 官方 → ghfast.top → gh-proxy.com → ghproxy.net（版本化文件名绕 CDN 缓存）
- 守护替换：运行中的 exe 被锁定无法覆盖，由独立 updater 脚本等旧进程退出后覆盖 → 自动重启 → 自删；**不影响 8099 dsh 服务进程**
- 沙盒模式：仅演练下载+校验到临时文件，绝不替换真实 exe

### ⚡ 代理检测全面升级（浏览器同源 · 启动不再卡 30 秒）

- **根因修复**：原实现逐个探测 15 个本地代理端口（每个 curl ~2s），扫描全程阻塞环境检测/日志/自动启动 → 无代理用户启动空转 30s+，launcher.log 迟迟不写 [env]。现改为：
  - ① 用户显式配置（活才用）→ ② **系统注册表代理**（Chrome/Edge 同源事实源，~0ms，不 curl）→ ③ 环境变量（活才用）→ ④ 端口扫描兜底
  - 慢路径不再逐个 curl 猜：先 TCP 监听检测（120ms/端口，不启动进程）只测真在监听的端口，整轮 3s 预算封顶
- **删除毒瘤**：探测结果绝不再自动写入 Cfg.Proxy 持久化——挂代理立即用上，断开代理立即失效，不会残留过期死代理

### 🧭 新手引导（给不懂技术的人看）

- **概览页橙色警告条**：环境没装好时页面顶部出现橙色横幅「还差 xxx 就装好了 + 一键安装按钮」，配大白话说明「不用懂技术，点按钮软件自动装好一切」
- **环境页引导卡升级**：橙色高亮边框 + 大字标题「🚀 还差一步就装好了」+ **四项缺失清单**（✅/❌ Node.js / npm / Git / dsh 逐项标出谁没装）+ 大号一键安装按钮
- 主按钮在环境缺失时变为「⚠ 一键安装」
- **引导卡改为四项任一缺失都显示**（此前仅缺 dsh 时显示，只缺 Git 的用户看不到引导）

### 🛠 一键安装 = 全环境补齐（缺什么装什么，已装不动）

- **Node.js**：缺才装（官方 → npmmirror → 华为云 三源）
- **Git**：自动安装——MinGit 多镜像下载（npmmirror 主源 + GitHub edge 加速 ghfast/gh-proxy/ghproxy 兜底）绿色解压到用户目录，写入 PATH，无需管理员、无需代理
- **dsh**：npm 安装，官方源 → npmmirror → 华为云 → 清除代理重试 共 5 级尝试
- **彻底摆脱系统 tar.exe 依赖**：Node/Git 解压改用纯 .NET ZipArchive（Win7/8/精简系统没有 tar.exe 也能装）
- 安装目录输入被清空 → 自动回退默认目录，不再静默取消
- 全程用户目录 + 用户环境变量，**不弹 UAC**，避免被杀软误判

### 🌐 公共镜像策略（无代理环境实测可用）

- **npm 源**：官方 → npmmirror（180ms）→ 华为云（509ms）
- **Node.js 下载**：官方 → npmmirror → 华为云
- **GitHub 加速（git 操作自动重试）**：直连失败自动依次尝试 ghfast.top → gh-proxy.com → ghproxy.net（`-c url.insteadOf` 一次性映射，不改用户配置；三者均实测支持 git 智能协议）
- **插件依赖安装**：npm install 失败自动回退 npmmirror → 华为云
- **启动器版本检查**：jsDelivr → raw.githubusercontent.com → ghfast 代理

### 📤 一键导出诊断包（新手报错求助核心工具）

- **修复诊断包必失败 bug**：原打包用 PowerShell Compress-Archive 子进程，桌面被重定向/无权限时打包静默失败，且 exit=0 误判成功 → 现在改用 **.NET ZipArchive 直写**（不依赖子进程/中文路径/桌面权限）
- **输出位置多级兜底**：桌面 → 我的文档 → 下载 → 用户目录 → exe 目录 → 系统临时目录，任一可写即成功
- 内含 launcher.log / dsh.log / crash.log / 脱敏 launcher.json / env-report.txt（环境报告 + 插件清单 + [env]/[install]/[npm]/[git] 关键行摘要）

### 📝 日志增强

- 启动检测（StartDetect）与手动重检（RunDetect）均写 `[env]` 摘要行（Node/npm/Git/dsh/port/proxy 全列出）
- 保存设置写 `[settings]` 行；端口切换写 `[port]` 行

### 🐛 修复

- **一键安装 Node.js 必失败**：下载地址解析用了"version 与 lts 字段必须相邻"的严格正则，而官方 index.json 两字段之间隔着十几个字段——**无论网络好坏都匹配失败**，报「无法获取 Node.js 下载地址」。改为宽松匹配 + npmmirror 优先
- **SmartHttp 空代理落到系统默认代理**：未配置代理时 HttpWebRequest 走 IE 系统代理，系统代理指向失效地址时全部请求挂死。改为空代理 = 显式直连
- **DownloadFile 同样问题**：WebClient 默认走系统代理，现在代理/直连双通道自动切换
- **git 死代理污染**：配置的代理失效时，git 操作先带死代理失败，转直连重试，再走 GitHub 加速镜像
- **代理误判**：TestProxy 现在校验真实响应内容（api.github.com/zen 返回 "zen"），防止 curl 报错输出被误判为代理可用
- **npm 安装超时收紧**：单次尝试 10 分钟 → 5 分钟，避免慢网死等 50 分钟
- **GitHub release 下载可能拿到 CDN 缓存的旧文件**：发布改用版本化文件名（DeepSeekHarness-v1.0.8.exe），上传后哈希核验
- **隐藏测试参数 --sandbox**：隔离测试模式（独立 Mutex、检测只信 PATH、禁止真实安装/PATH 写入），正常用户无感知

## v1.0.7 (2026-08-16)

**一键安装不再"哑巴失败"——真实报错全暴露 + 四连招重试**

### ✨ 一键安装 / 升级 dsh 诊断升级

- **暴露 npm 真实报错**：以前 npm 安装失败只提示「失败（超时）」，现在会把 npm 的真实错误（`npm ERR! code xxx` 等）直接显示在弹窗里，并完整写入 launcher.log——下次再失败，一眼就能看出是网络、代理还是 npm 环境问题
- **四级尝试链**：官方源 → 国内镜像 → 清除代理重试官方源 → 清除代理重试国内镜像。**修复"npm 配置/环境变量里残留失效代理 → 所有源都连不上"的典型翻车场景**（npm 不读系统代理，只认自己的 proxy 配置，指向已失效的 127.0.0.1 代理时官方源和镜像全挂）
- **加速与防挂起**：安装加 `--no-audit --no-fund --fetch-timeout=60s`，慢网/死网时不再干等 5 分钟才报错；单次超时上限放宽到 10 分钟
- **安装后核验**：npm 声称成功后会实际检查 `dsh.cmd` / 包目录是否存在，防止「假成功」；并实时把 npm 全局目录写入 PATH（用户环境变量 + 当前进程），**装完不用重启启动器，立刻能检测到 dsh**
- **装完提示 Git**：环境装完但未检测到 Git 时，弹窗提示「插件安装/更新需要 Git，建议前往 git-scm.com 安装」

### 🐛 修复

- npm 安装失败误报「超时」（实际是网络错误 / 依赖解析失败等）——现在显示真实原因

## v1.0.6 (2026-08-15)

**三道防线：插件自检升级（不止拦缺依赖）**

### 🛡️ 插件自检从「静态预检」升级为「三道防线」

- **① 静态依赖预检**（启动前）：扫描插件 package.json 依赖，缺失自动从共享依赖池补齐，修不好先隔离
- **② 日志巡检**（服务就绪后）：扫描 dsh 日志尾部，正则匹配 `failed to load` / `cannot find package` / `failed to import` 等加载期错误，自动定位问题插件并隔离——**覆盖插件代码 bug、API 不存在、版本不兼容等静态检查不出的问题**
- **③ 前端 bundle 探测**：对每个插件请求 `client.js`，5xx 视为前端 UI 加载失败并隔离——**覆盖"一个坏插件拖垮整个前端"的事故场景**（404 视为服务端插件无 UI，正常不误伤）
- 三道防线结果统一弹窗提示，附 dsh 修复建议

### 🐛 修复

- 修正 bundle 探测误伤：404（无前端 bundle 的服务端插件）不再误判为失败，仅 5xx 才隔离

## v1.0.5 (2026-08-15)

**启动步骤动画 & 智能环境定位**

### ✨ 一键启动步骤动画

- 点击「一键启动」后，概览页显示**动态步骤指示器**：① 检查插件 → ② 启动服务 → ③ 就绪 ✓
- 当前步骤圆点旋转动画，完成步骤变绿打勾，就绪后自动收起——**交代清楚每一步在干什么**，不再静默等待

### 🔍 智能环境定位（针对源码版 dsh 用户）

- 检测不到 dsh 时环境页出现**引导卡**：「✅ 我已安装 dsh / 🆕 一键安装」
- **智能定位 dsh**：深度扫描（where dsh → npm 全局 → 常见安装位置 → PATH 全扫），**支持源码版 dsh**（`dsh.cjs` 入口，如 `pnpm dsh` 安装方式）——找到即写入配置，无需再装全局 npm 包
- **手动选择 dsh 文件** / **指定目录自动递归查找**（跳过 node_modules、限深防卡死）

### 🐛 修复

- 最小化/托盘恢复后部分区域不重绘（Win11 22H2 侧边栏/标题栏空白）：淡入动画释放句柄 + 恢复时强制重新合成

## v1.0.4 (2026-08-15)

**「全部更新插件」只更新真正有更新的插件**

### 🐛 修复

- **「全部更新插件」不再列出无关插件**：此前会把所有 git 插件都列进结果弹窗（包括"已是最新"的），现在改为**只处理确有更新的插件**——先 fetch 当前分支计算落后提交数，无更新的一律不出现、不执行任何操作；结果弹窗只显示「✨ 已更新」或「⚠️ 失败」
- **本地分支与远程默认分支不一致的兜底**：本地 `master` / 远程 `main` 这类场景，fetch 当前分支失败时自动对比远程默认分支 HEAD，一致即视为已最新（不再误报"无法连接远程"）
- 全部已最新时提示「全部插件已是最新 ✓」

## v1.0.3 (2026-08-15)

**环境引导 & 渲染稳定性**

### 🔍 智能环境检测引导

- **引导卡**：检测不到 dsh 时，环境页顶部出现引导卡「我已安装 dsh / 一键安装」+ 高级定位入口
- **智能定位 dsh**：一键深度扫描（`where dsh` → npm 全局目录 → 常见安装位置 → PATH 全扫），支持**源码版 dsh**（`dsh.cjs` 入口，如 D:\software\deepseekharness 这类 `pnpm dsh` 安装）——找到即写入配置并重新检测，无需再装全局 npm 包
- **手动选择 dsh 文件**：文件选择器直接指定 dsh.cmd/dsh.exe/dsh.cjs
- **指定目录自动查找**：选一个文件夹，软件递归遍历自动定位 dsh（跳过 node_modules 深层，限深防卡死）

### 🐛 修复

- **最小化/托盘恢复后部分区域不重绘**（Win11 22H2 等系统侧边栏/标题栏空白，点交互才恢复）：启动淡入动画完成后释放动画句柄（不再钉住 Opacity）+ 恢复时强制重新合成（ShowMain / StateChanged 双保险）

## v1.0.2 (2026-08-15)

**交互修复与一致性打磨**

### 🐛 修复

- **托盘主题切换全局生效**：托盘菜单切深浅主题后，**整个主窗口立即重建并弹出展示**新主题（此前只更新了菜单自身）；切换动作先执行再关闭弹层，避免相互干扰
- **托盘菜单点击崩溃加固**：所有菜单项"先执行动作再关闭"，任意操作异常都被捕获，**不再出现点击某项后程序直接退出**；主窗口隐藏时打开插件商城会先弹出主窗口（避免 owned window 异常）
- **更新页与插件页判定统一**：`PullPlugin`（插件页"检查更新"）与 `CheckUpdates`（更新页"可更新"）改用**完全一致的判定策略**——`fetch 当前分支 → 计算本地落后提交数`，只有真正落后才显示可更新/执行拉取；不再用陈旧的 `@{u}` 引用误判"已最新"，也不再哈希不同就误报"可更新"
- **git 操作自动走代理**：`RunGit` 自动附加 `-c http.proxy`，第三方插件仓库（需代理访问）的检查/拉取更可靠
- **更新页版本显示**：修复"当前 v1.0.1、最新 1.0.0"的版本号不同步问题，统一到 1.0.2

### ✨ 其他

- 设置页新增「ℹ️ 关于」对话框：中英双语介绍（软件定位、核心功能、8 语言）
- README 重写：突出"管理 dsh/插件、一键更新维护、小白友好"亮点，新增多语言文档与对应语言界面配图

## v1.0.1 (2026-08-15)

**插件依赖管理系统** — 根治"装个插件缺依赖 → 整个服务崩溃 → 前端全挂"的连锁故障。

### 🧩 插件依赖管理（本次核心）

- **安装后自动装依赖**：git/npm 安装插件完成后，自动解析 package.json → 缺失依赖自动从共享目录链接或 npm 补齐，从源头杜绝"装完就崩"
- **依赖状态徽章**：插件列表每行显示 `✓ 依赖完整` / `⚠️ 缺依赖: xxx`，一眼识别高危插件
- **一键修复依赖**：缺依赖的插件自动出现「🔧 修复依赖」按钮；「一键维护」同步升级为智能依赖检查（不再盲目 npm install）
- **坏插件自动隔离**：修复不了的插件自动挂 `.disabled` 后缀隔离，**保证服务正常启动**，不再拖垮整个插件树
- **玩家提示 + dsh 修复命令**：隔离后弹出提示，附上可执行的修复命令（`npm install -g <依赖>` / `cd <插件目录> && npm install`）
- **共享依赖目录**：`plugins/_shared-deps` junction 池统一复用 dsh 自带依赖（schemastery/cordis/react 等），一处依赖多处复用
- **智能排除内部目录**：`_shared-deps` 等下划线开头目录不再被误认为插件
- **支持嵌套 package.json**：插件依赖声明在子目录（`dsh-vision-toolkit/`、`injector/` 等）时也能正确识别

### 🐛 修复

- 修复插件缺依赖时服务端插件树崩溃、前端所有 client bundle 加载失败的连锁故障
- 修复全局 npm 目录探测（兼容 D:\npm-global / %USERPROFILE%\npm-global / %APPDATA%\npm）
- 修复 scoped 包（@scope/name）依赖在共享目录中的解析

## v1.0.0 (2026-08-15)

**正式版首发** — WPF 重构版（GPU 渲染 / 双主题 / 现代交互），此前 WinForms 试验版全部历史归档清理。

### 🎨 界面与质感

- **WPF 全面重写**：GPU 合成渲染、PerMonitorV2 任意 DPI 高清、无边框窗原生缩放/最大化/吸附（WindowChrome）
- **深色 / 浅色双主题**：设置页与托盘一键热切换，全控件主题感知，无白底白字/对比度崩塌
- **DeepSeek 品牌质感**：官方蓝渐变圆角 Logo（白鲸/蓝鲸随主题）、深海蓝调色、微渐变卡片、1px 高光边框、柔和投影
- **丝滑动效**：切页阻尼淡入上滑、导航项指示条展开、按钮悬停光晕、启动闪屏流光 + 白鲸光晕
- **现代系统托盘**：WPF 悬浮圆角卡片菜单（打开/启动/停止/重启/浏览器/商城/主题/退出），告别 WinForms 原生旧菜单
- **现代化弹窗**：全部提示/确认替换为深浅自适应的圆角微光对话框（✨/⚠️/❓ 图标标题）
- **日志页重构**：终端式深色代码区、源切换、实时搜索过滤、复制、清空；修复顶部空白断层
- 极简自适应滚动条、修复下拉框白底白字/蓝白割裂、修复浅色模式刺眼眩光

### ⚙️ 功能

- **一键安装（自定义路径）**：自动下载 Node.js（官方→镜像兜底），支持指定安装目录，自动持久化写入用户 PATH 与 launcher.json，指令全局可用
- **常驻服务状态监控器**：启动/停止/重启后 UI 与真实端口状态永远同步（修复"服务已重启但界面卡在未启动"）
- **插件智能更新**：仅当远程确有新提交才拉取（`@{u}` 落后数判定 + 无上游时对比远程默认分支），已最新显示 ✓ 绝不误报失败
- **更新页重构**：明确标注「🚀 启动器 (Launcher)」「🐋 Harness (dsh)」「📦 插件」，检查更新带转圈动画，按钮状态感知（已最新置灰），更新完成后立即刷新
- **插件商城多源聚合**：GitHub 多关键词检索 + npm 官方包源 + Awesome 列表，数百插件；已安装自动标记 ✓ 不可再点
- **环境检测增强**：nvm/scoop/choco/注册表 PATH 多策略自愈；环境未检测完成时点击主按钮先检测而非误判"未安装"
- **多语言**：中 / 英 / 日（国旗图标切换）
- **语义化版本比较**：启动器自更新改用 Version 语义比较，告别字符串硬编码误判
